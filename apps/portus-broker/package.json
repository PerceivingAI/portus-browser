{
  "name": "@portus/broker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "bin": {
    "portus-broker": "./bin/portus-broker.js"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test tests/*.test.mjs",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@portus/config": "workspace:*",
    "@portus/events": "workspace:*",
    "@portus/protocol": "workspace:*",
    "@portus/recipes": "workspace:*",
    "@portus/transport": "workspace:*",
    "zod": "^4.3.6"
  }
}
